{
  "name": "bela-climb",
  "version": "1.0.0",
  "private": true,
  "description": "Bela Climb — jogo de plataforma vertical (HTML5/Canvas/JS) empacotado para iOS via Capacitor",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "sync": "npm run build && cap sync ios",
    "ios": "npm run sync && cap open ios"
  },
  "keywords": [
    "game",
    "capacitor",
    "ios"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  }
}
